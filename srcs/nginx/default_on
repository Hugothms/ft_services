
server {
	#listen 443 ssl;
	#listen [::]:443 ssl;
	listen 80 default_server;
	listen [::]:80 default_server;

	root /var/www/html;

	index index.htm index.html index.nginx-debian.html;

	server_name _wp;

	location / {
		autoindex on;
		try_files $uri $uri/ =404;
	}
}
