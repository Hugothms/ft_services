
server {
	listen 443 ssl;
	listen [::]:443 ssl;
	listen 80 default_server;
	listen [::]:80 default_server;
    return 301 https://$host$request_uri;

	root /var/www/html;

	index index.html;

	server_name _;

	location / {
		autoindex on;
		try_files $uri $uri/ =404;
	}
}
